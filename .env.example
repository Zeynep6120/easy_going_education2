# Copy to `.env` and set values before starting the server.
# You can set either `DATABASE_URL` (common for hosting) or `PG_CONNECTION` (used in this project).
# Example local DB (replace user, password, host, port, dbname):
DATABASE_URL=postgresql://dbuser:dbpass@localhost:5432/modul33db
# Or alternate name used in code (uncomment if you prefer):
# PG_CONNECTION=postgresql://dbuser:dbpass@localhost:5432/modul33db

# Server port (optional)
PORT=3000
